import styled from "styled-components"

export const Container = styled.div`
    margin-top: 40px;

    span {
        font-size: 18px;
        font-weight: bold;
        color: var(--secondary-color);
        margin-left: 10px;
    }
`
