import styled from "styled-components"

export const Container = styled.div`
    margin-top: 5px;

    span {
        font-size: 18px;
        font-weight: bold;
        color: var(--secondary-color);
        margin-left: 10px;
    }
`
